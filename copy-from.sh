#!/bin/bash

TARGET_DIR="files/"

cp -p ~/.bashrc $TARGET_DIR
cp -p ~/.bash_profile $TARGET_DIR
cp -p ~/.tmux.conf $TARGET_DIR
#cp -p ~/.vimrc $TARGET_DIR
cp -p ~/.gitconfig $TARGET_DIR
cp -p ~/.config/starship.toml $TARGET_DIR
